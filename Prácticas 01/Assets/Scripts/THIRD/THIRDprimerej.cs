using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class THIRDprimerej : MonoBehaviour
{

    void Saludo (string hola)
    {
        print("hola " + hola);
    }



    // Start is called before the first frame update
    void Start()
    {
        Saludo("zapato");
        Saludo("chancla");

    }



    // Update is called once per frame
    void Update()
    {
        
    }
}
