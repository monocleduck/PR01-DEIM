using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SECprimerej : MonoBehaviour
{

    public int bucle1 = 1;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (bucle1 != 100)
        {

            print(bucle1);

            bucle1++;
        }
    }
}
